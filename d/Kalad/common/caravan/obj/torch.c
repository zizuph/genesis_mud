inherit "/std/torch";
inherit "/lib/keep";
void
create_torch()
{
    set_name("torch");
    set_pname("torches");
    set_adj("standard");
    set_long("A typical torch, there is nothing outstanding about it.\n");
    set_strength(3);
    set_short("standard torch");
    set_pshort("standard torches");
    set_keep();
}
