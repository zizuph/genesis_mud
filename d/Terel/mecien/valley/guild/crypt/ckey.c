/* Creative rights reserved to Mecien(Jeff Babcock) and team. */
inherit "/std/key";

create_key() {
  ::create_key();

    set_name("key");
    add_name("crypt_key");
   set_adj("black");
  add_adj("iron");
set_long("A long black iron key, the handle looking like a serpent.\n");

   set_key(909090);
}
