/* Creative rights reserved to Mecien(Jeff Babcock) and team. */
inherit "/std/object";
#include <stdproperties.h>

create_object()
{
   set_name("crystal");
   set_adj("blue");
   add_name("mystic_quest_crystal");
   set_long("A small piece of crystal, it is pale blue.\n");
   add_prop(OBJ_I_VALUE, 45);
}

