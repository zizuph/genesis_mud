/* Creative rights reserved to Mecien(Jeff Babcock) and team. */
inherit "/std/object";
#include <stdproperties.h>

create_object()
{
   set_name("ash");
   set_adj("bit of");
   add_name("mystic_quest_ash");
   set_long("A bit of ash, soft and fluffy as if from paper or papyrus.\n");
   add_prop(OBJ_I_VALUE, 0);
}

