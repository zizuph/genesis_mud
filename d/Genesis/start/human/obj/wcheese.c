/*
 * Copyright (C) 10-December-2000 by Shadow DeForest.
 *
 * Creator  : Shadowlynx
 * Date     : 10-December-2000
 * Filename : wcheese.c
 * Purpose  : A wedge of cheese commonly used for trail snacks.
 * Credits  : Modified from the original file of
 *              1. </d/Avenir/common/dark/obj/cheese.c>
 * Includes : Food sold by the Inn located in Traveller Camp.
 *            To be generated by the charity box in Sparkle.
 *            Generates random sizes, descriptions and names.
 * Updates  :
 */

inherit "/std/food";
inherit "/lib/keep";

#include <stdproperties.h>

void
create_food()
{
    set_amount(350);
    set_name("cheese");
    add_name(({"wedge","food"}));
    add_pname(({"food","foods","cheeses"}));
    add_adj(({"hard", "round"}));   
    set_short("round of hard cheese");
    set_long("This is a round of hard white cheese. It is "+
      "a bit chalky and hasn't much flavour, but it is high"+
      "ly nutritious.\n");
    set_keep(1);
    add_prop(OBJ_I_VALUE, 210);
}

